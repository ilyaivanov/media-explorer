export const levelOffsetForMenu = 15;
export const rowLeftPadding = 8;
export const dropDestinationLineHeight = 2;
// Below are variables references from CSS (always change together)
// or consider pros/cons of using Styled Components to mitigate this problem if it will rise
export const chevronWidth = 20;
export const menuHeight = 50;
